package game.player;

public interface Attacks {
	public default String legDropKick(int moveSuccess) {
		String moveResults = "Hmmm....Something must have gone wrong with the pin hold move";
		return moveResults;
		//needs switch
	}

}
